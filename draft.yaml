models:
  Account:
    owner_id: id:users
    provider: string # discord
    provider_id: string # discord: snowflake id
    nickname: string # discord: $username#$discriminator
    name: string
    email: nullable string
    avatar: string # url to avatar
    extra: json
  Order:
    account_id: id:accounts
    status: string
    status_reason: nullable string
    total_cost: decimal
  VBuck:
    order_id: id:orders
    amount: int
    delivered_at: nullable timestamp
    delivered_by: nullable id:users

controllers:
  # ...
seeders: Account
